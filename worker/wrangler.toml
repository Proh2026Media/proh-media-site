name = "proh-gemini-proxy"
main = "src/index.js"
compatibility_date = "2024-11-01"

# A chave da API NÃO fica aqui. Ela é um "secret", definido com:
#   npx wrangler secret put GEMINI_API_KEY
# ou pelo painel do Cloudflare (Settings > Variables > Secret).
